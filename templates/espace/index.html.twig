<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>{% block title %}Votre espace{% endblock %}
		</title>
		<link
		rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		{% block stylesheet %} {{ encore_entry_link_tags('espace') }}
        
		{% endblock %}

		{% block javascripts %} {{ encore_entry_script_tags('espace') }}
        
		{% endblock %}
		
	</head>

{% set url = ""%}
{% block body %} 

  	<body>
   		
 			<aside class="bg-light">
	  			{% include "_menu_left.html.twig" %}
			</aside>
			<div class="text-center">
			<h1>Tes dernières infos</h1>
			<main role="main" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4">
          <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
           
		   {% if news is defined %}
				{% for new in news %}
					{{new|raw}}
				{% endfor %}
			{% endif %}
			</div>
			
			<h2>Ici découvre des nouveaux flux !</h2>
           
              <form method="GET" action="" class="d-flex form-control-sm">
			
				<input type="text" class="form-control" name="url" placeholder="Tape l'url du site que tu souhaite suivre ici. Exemple : https://www.developpez.com/" value="{{url}}">
			
				<button type="submit" class="btn btn-outline-secondary">Rechercher</button>
				<a href=" {{path("espace_effacer_recherche")}} " class="btn btn-outline-danger">Effacer</a>

			</form>
			
			{# Si l'utilisateur veut ajouter le flux à sa liste #}
			{% if form is defined %}
			<div class="form-control mt-3">
			<h3>Ajouter un flux à ma liste</h3>
				{{ form_start(form) }}
				{{ form_widget(form) }}
				<button class="btn styled w-25 mt-3">{{ button_label|default('Save') }}</button>
				{{ form_end(form) }}
			</div>
				{{flux|raw}}

			{% endif %}

			{# Si l'utilisateur affiche le flux le bouton ajouter apparait #}
			<div>
			{% if flux %}
				<form method="GET" action="">
					<input type="hidden" name="ajout_url" value="{{(url_ajout ?? url)}}">
					<button type="submit" class="btn btn-outline-primary">Ajouter à ma liste</button> 
				</form>
				{{flux|raw}}
			{% else %}
				<p class="mt-3"> FeedyTech t'accompagne dans ta veille 😉</p>
			{% endif %}
			</div>
			</div>
		</div>
		</div>
			{% endblock %}
	</body>
</html>
